@model NotificationBadgeViewModel

@if (Model.Count > 0)
{
    <span class="e-badge e-badge-notification e-badge-overlap @Model.Color"
          style="position:absolute; top:@Model.PositionTop; right:@Model.PositionRight;">
        @Model.Count
    </span>
}
